function main() {
  var myCalendar = CalendarApp.getCalendarById('your_calendar_id');
  var now = new Date();
  var twoWeeksFromNow = new Date(now.getTime() + (14 * 24 * 60 * 60 * 1000));
  var events = myCalendar.getEvents(now, twentyFourHoursFromNow);
  for(i in events) {
    var guests = events[i].getGuestList();
    for(x in guests){
        var target_email = guests[x].getEmail();
        MailApp.sendEmail(target_email, "件名", "本文");
    }
  }
}
